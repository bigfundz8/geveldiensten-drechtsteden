/* Alleen jij ziet de dashboards (Cloudflare / Google Analytics-account).
   Bezoekers zien deze gegevens niet op de website.
   Vul hieronder je codes in en push/deploy — daarna werkt tracking. */
window.GDD_ANALYTICS = {
  /* Google Analytics 4 — bv. "G-XXXXXXXXXX" */
  ga4: "G-0XXRNFY4N4",
  /* Cloudflare Web Analytics: al via Automatic setup in Cloudflare — geen token nodig */
  cloudflare: "",
};
